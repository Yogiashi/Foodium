<div class="container">
  <div class="row">
    <div class="col-md-6 col-12 mx-auto">
      <h1 class="responsive-title text-center mb-5">フォロー中</h1>
      <% if @users.exists? %>
        <table class="table table-borderless text-center">
          <% @users.each do |user| %>
            <tr>
              <td style="vertical-align:middle;">
                <%= link_to user_path(user) do %>
                <%= image_tag user.get_profile_image, size: '60x60', class: "rounded-circle" %>
                <% end %>
              </td>
              <td style="vertical-align:middle;"><%= link_to user.name, user_path(user), class: "text-decoration-none text-dark" %></td>
              <td id="follow_btn_<%= user.id %>" style="vertical-align:middle;">
               <%= render 'public/relationships/btn', user: user %>
              </td>
            </tr>
          <% end %>
        </table>
      <% else %>
        <h3 class="text-center mt-5">ユーザーはいません</h3>
      <% end %>
    </div>
  </div>
</div>
